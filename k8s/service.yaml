apiVersion: v1
kind: Service
metadata:
  name: simple-api-proxy
  labels:
    app: simple-api-proxy
spec:
  type: ClusterIP
  selector:
    app: simple-api-proxy
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
